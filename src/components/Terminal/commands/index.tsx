import { catCmd, clearCmd, cmdCd, cmdEcho, lsCmd, pwdCmd, rmCmd, shrugCmd, treeCmd, perlCmd } from './commands';
import cowsay from './cowsay';

export default [cmdEcho, cmdCd, catCmd, pwdCmd, lsCmd, treeCmd, clearCmd, shrugCmd, rmCmd, cowsay, perlCmd];
