import { catCmd, clearCmd, cmdCd, cmdEcho, lsCmd, pwdCmd, rmCmd, shrugCmd, treeCmd } from './commands';
import cowsay from './cowsay';

export default [cmdEcho, cmdCd, catCmd, pwdCmd, lsCmd, treeCmd, clearCmd, shrugCmd, rmCmd, cowsay];
